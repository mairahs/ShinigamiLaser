services:
    _defaults:
        autowire: true
#    user.example:
#        class: UserBundle\Example
#        arguments: ["@service_id", "plain_value", "%parameter%"]

    UserBundle\Manager\PlayerManager:
        public: true

    UserBundle\DoctrineListener\AvatarUploadListener:
        public: true
        arguments: ['%kernel.project_dir%']
        tags:
            - { name: doctrine.event_listener, event: postPersist }

    UserBundle\DoctrineListener\TokenGenerator:
        public: true
        tags:
            - { name: doctrine.event_listener, event: prePersist}

    UserBundle\Manager\MailManager:
        public: true
        arguments: ['@mailer', '%mail_admin%','@templating.engine.twig']

    UserBundle\DoctrineListener\SendMailActivation:
        public: true
        tags:
            - { name: doctrine.event_listener, event: postPersist}
