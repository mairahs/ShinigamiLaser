{% extends '::base.html.twig' %}

{% block body %}
    <h3>Editer son avatar</h3>
    <img src="{{ asset('upload/' ~ player.avatar.name ~ '.' ~ player.avatar.extension) }}" class="img_rounded">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}