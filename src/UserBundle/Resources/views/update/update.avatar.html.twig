{% extends '::base.html.twig' %}

{% block body %}
    <h3>Editer son avatar</h3>
    {% include('inc/inc.avatar.img.html.twig') %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}