{% extends '::base.html.twig' %}

{% block body %}
    <h3>Editer son mot de passe</h3>
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}

